# Task Management System

Build a task management system with:
- User authentication (email + password, JWT-based sessions)
- Create, read, update, delete tasks
- Each task has: title, description, status (todo / in-progress / done), priority, due date
- Filter and sort tasks by status, priority, due date
- Assign tasks to users within a team
- REST API with proper status codes and pagination
